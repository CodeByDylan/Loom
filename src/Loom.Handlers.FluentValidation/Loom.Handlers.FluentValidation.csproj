<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <Description>A decorator that validates requests with FluentValidation before a handler runs, returning an Invalid failure instead of throwing.</Description>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\Loom.Handlers\Loom.Handlers.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="FluentValidation" />
    </ItemGroup>

</Project>
