<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <Description>Registers handlers and wraps them in an explicit, ordered decorator chain. Deliberately has no dispatcher: consumers inject IHandler directly.</Description>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\Loom.Handlers.Abstractions\Loom.Handlers.Abstractions.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
        <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    </ItemGroup>

</Project>
